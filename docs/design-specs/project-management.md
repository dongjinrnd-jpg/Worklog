# 프로젝트 관리 페이지 설계 문서

## 1. 개요

프로젝트 관리 페이지는 사용자가 프로젝트 정보를 등록, 조회, 수정할 수 있는 전용 페이지입니다. 이 페이지를 통해 개발 프로젝트의 단계, 고객사, 담당자, ITEM, PART NO 등의 핵심 정보를 효과적으로 관리하고, 업무진행 상태를 추적할 수 있습니다. 또한 프로젝트별 업무진행 사항과 애로사항을 별도 페이지에서 관리하고, 변경 이력을 자동으로 기록하여 프로젝트의 진행 과정을 투명하게 관리합니다. Google 스프레드시트를 백엔드로 활용하여 데이터를 저장하고 불러오는 기능을 제공합니다.

---

## 2. 기능 요구사항

### 필수 기능
- 프로젝트 정보 등록 폼 제공
  - NO, 진행여부, 고객사, 소속, 모델, ITEM, PART NO, 개발담당, 개발업무단계, 대일정, 현재단계, 업무진행사항, 애로사항, 비고, 업무추가 일정계획, 판매가, 재료비, 재료비율 입력 필드 구현
- 개발담당자 다중 선택 기능
  - `담당자` 시트 데이터 활용
  - 직급과 이름 함께 표시
  - 검색 기능으로 담당자 빠르게 찾기
- 프로젝트 목록 표시 및 필터링
  - 테이블 형태로 프로젝트 목록 표시
  - ITEM, PART NO, 고객사, 개발담당, 업무단계, 현재단계 중심 표시
  - 다양한 필드 기준 필터링 및 정렬
- 프로젝트 정보 수정 기능
  - 기존 프로젝트 선택하여 정보 수정
  - 변경 내역 추적 및 저장
- 프로젝트 상세 정보 보기
  - 모든 필드 정보를 한눈에 볼 수 있는 상세 뷰
  - 확장 가능한 텍스트 영역 (긴 내용 처리)
- 유효성 검증
  - 필수 입력 필드 검증
  - 입력 형식 검증 (날짜, 텍스트 길이 등)
- 성공/실패 피드백 UI
  - 저장, 수정, 삭제 등 작업 결과 피드백
- 프로젝트 이력 관리
  - 프로젝트 수정 시 변경된 내용 자동 기록
  - 날짜, ITEM, PART NO, 업무진행사항, 업무추가 일정계획, 애로사항, 애로사항 개선 여부 저장
  - `프로젝트이력관리` 시트에 누적 기록
- 프로젝트별 업무진행 사항 및 애로사항 관리
  - 별도 페이지에서 프로젝트별 세부 정보 관리
  - 업무진행사항, 업무추가 일정계획, 애로사항 입력 및 조회

### 선택 기능
- 프로젝트 복제 기능 (새 프로젝트 빠르게 생성)
- 템플릿 기반 프로젝트 생성
- 프로젝트 목록 엑셀/CSV 내보내기
- 프로젝트 상태에 따른 시각적 표시 (예: 완료, 진행 중, 지연됨)
- 담당자별 프로젝트 필터링 및 그룹화
- 프로젝트 히스토리 보기 (변경 이력)
- 대시보드 연동 (요약 정보 표시)

---

## 3. UI/UX 설계

### 페이지 레이아웃
```
+------------------------------------------+
|             헤더/네비게이션              |
+------------------------------------------+
|                                          |
|        [새 프로젝트 등록] 버튼           |
|                                          |
+------------------------------------------+
| 필터 영역                                |
| +--------+ +--------+ +--------+ +-----+ |
| | ITEM   | | 고객사 | | 담당자 | | ... | |
| +--------+ +--------+ +--------+ +-----+ |
+------------------------------------------+
| 정렬 옵션 | 뷰 옵션 | 다운로드 | 초기화  |
+------------------------------------------+
|                                          |
|            프로젝트 목록 테이블          |
|                                          |
| +---+------+------+-------+-------+----+ |
| | ▶ | ITEM | PART | 고객사| 담당자| .. | |
| +---+------+------+-------+-------+----+ |
| | ▼ | ITEM | PART | 고객사| 담당자| .. | |
| +---+------+------+-------+-------+----+ |
| |   | 확장된 행 - 세부 정보 표시      | |
| |   | 기본 프로젝트 정보             | |
| |   | [수정하기] [복제하기] [업무관리]| |
| +---+------+------+-------+-------+----+ |
| | ▶ | ITEM | PART | 고객사| 담당자| .. | |
| +---+------+------+-------+-------+----+ |
|                                          |
+------------------------------------------+
|            페이지네이션                  |
+------------------------------------------+
```

### 새 프로젝트 등록 모달
```
+------------------------------------------+
|    새 프로젝트 등록     |      X        |
+------------------------------------------+
| 진행여부*:     | [라디오 버튼]          |
|               | ● 진행 ○ 보유 ○ 완료    |
+------------------------------------------+
| 고객사*:       | [고객사 입력 필드]     |
+------------------------------------------+
| ITEM*:         | [ITEM 입력 필드]       |
+------------------------------------------+
| PART NO:       | [PART NO 입력 필드]    |
+------------------------------------------+
| 개발담당:      | [담당자 다중 선택]     |
+------------------------------------------+
| 개발업무단계:  | [업무단계 다중 선택]   |
|               | (프로젝트 단계목록 시트 참조) |
+------------------------------------------+
| 대일정:        | [날짜 범위 선택기]     |
+------------------------------------------+
| 판매가:        | [숫자 입력 필드]       |
+------------------------------------------+
| 재료비:        | [숫자 입력 필드]       |
+------------------------------------------+
| 재료비율:      | [숫자 입력 필드] %     |
+------------------------------------------+
|                                          |
| [저장하기]     [취소]     [초기화]       |
+------------------------------------------+
```

### 프로젝트 수정 인라인 폼 (확장된 행에서)
```
+------------------------------------------+
| ITEM: ABC    | PART NO: XYZ | 고객사: 도연건설  | 
+------------------------------------------+
| 진행여부:    | [라디오 버튼]              |
|              | ○ 진행 ● 보유 ○ 완료       |
+------------------------------------------+
| 개발담당:     | [담당자 다중 선택]        |
+------------------------------------------+
| 개발업무단계: | [업무단계 다중 선택]      |
+------------------------------------------+
| 대일정:       | [날짜 범위 선택기]        |
+------------------------------------------+
| 판매가:       | [숫자 입력 필드]          |
+------------------------------------------+
| 재료비:       | [숫자 입력 필드]          |
+------------------------------------------+
| 재료비율:     | [숫자 입력 필드] %        |
+------------------------------------------+
|                                          |
| [저장]   [취소]   [복제하기]   [업무관리] |
+------------------------------------------+
```

### 프로젝트 업무관리 페이지
```
+------------------------------------------+
|             헤더/네비게이션              |
+------------------------------------------+
|                                          |
| 프로젝트: TY CAB TILT SYSTEM(표준차)     |
| PART NO: AD62203013                      |
|                                          |
+------------------------------------------+
| 업무진행사항:                           |
| [다중 라인 텍스트 필드]                 |
+------------------------------------------+
| 업무추가 일정계획:                      |
| [다중 라인 텍스트 필드]                 |
+------------------------------------------+
| 애로사항:                               |
| [다중 라인 텍스트 필드]                 |
+------------------------------------------+
| 애로사항 개선 여부: [체크박스] ○         |
+------------------------------------------+
|                                          |
| [저장하기]     [취소]     [목록으로]     |
+------------------------------------------+
|                                          |
|          업무진행 이력 테이블           |
|                                          |
| +--------+---------------+------------+ |
| | 날짜   | 업무진행사항  | 애로사항   | |
| +--------+---------------+------------+ |
| | 04-15  | 본양산 진행중 |            | |
| +--------+---------------+------------+ |
| | 04-13  | 본양산 진행중 | 누유 발생..|✓|
| +--------+---------------+------------+ |
| | 04-12  | 초도양산완료  | 누유 발생..| |
| +--------+---------------+------------+ |
|                                          |
+------------------------------------------+
```

### 주요 컴포넌트

1. **필터 패널**
   - ITEM 필터 (텍스트 검색/드롭다운)
   - 고객사 필터 (텍스트 검색/드롭다운)
   - 담당자 다중 선택 필터 (체크박스 리스트)
   - 업무단계 필터 (라디오 버튼/드롭다운)
   - 현재단계 필터 (라디오 버튼/드롭다운)
   - 적용된 필터 태그 표시 및 개별 삭제

2. **결과 컨트롤 바**
   - 정렬 옵션 드롭다운 (ITEM순, 고객사순, 단계순 등)
   - 뷰 옵션 토글 (표 형식/카드 형식)
   - 결과 다운로드 버튼 (엑셀/CSV)
   - 필터 초기화 버튼
   - 새 프로젝트 등록 버튼

3. **프로젝트 목록 테이블**
   - 각 행 앞에 확장/축소 아이콘(▶/▼) 배치
   - 기본 컬럼: ITEM, PART NO, 고객사, 개발담당, 업무단계, 현재단계, 판매가, 재료비, 재료비율
   - 확장 시 추가 표시 항목: 기본 프로젝트 정보
   - 확장된 행에 수정 폼 표시
   - 각 행에 복제 버튼
   - 각 행에 업무관리 버튼 (업무진행 사항 및 애로사항 관리 페이지로 이동)
   - 컬럼 헤더 클릭 시 정렬 기능
   - 필요 시 컬럼 너비 조정 기능

4. **프로젝트 등록 폼**
   - 저장 시 NO 자동 할당 (현재 프로젝트에 등록된 가장 큰 번호 + 1)
   - 필수 필드 표시 (*)
   - 진행여부 선택 (라디오 버튼, 기본값: 진행)
   - 고객사 및 ITEM 필수 입력 필드
   - PART NO 선택적 입력 필드 
   - 개발담당 다중 선택 컴포넌트
   - 개발업무단계 다중 선택 컴포넌트 (프로젝트 단계목록 시트 참조)
   - 대일정 날짜 범위 선택기
   - 저장/취소/초기화 버튼

5. **프로젝트 수정 폼**
   - 인라인 수정 방식 (확장된 행에서 바로 수정)
   - 기존 데이터 미리 로드 (기본값으로 설정)
   - 진행여부 변경 가능
   - 모든 필드 수정 가능 (ITEM, PART NO, 고객사 포함)
   - 개발업무단계 다중 선택 업데이트
   - 긴 텍스트 필드 (업무진행사항, 애로사항, 비고, 업무추가 일정계획) 수정
   - 저장/취소/복제 버튼
   - 수정 시 데이터 유효성 검증

6. **프로젝트 업무관리 페이지**
   - 특정 프로젝트의 업무진행사항, 업무추가 일정계획, 애로사항 관리
   - 프로젝트 기본 정보 표시 (ITEM, PART NO 등)
   - 업무진행사항, 업무추가 일정계획, 애로사항 입력 필드
   - 애로사항 개선 여부 체크박스
   - 저장 시 프로젝트 정보 업데이트 및 이력 저장
   - 이력 테이블 업데이트
   - 성공/실패 피드백 제공

7. **이력 관리 컴포넌트**
   - 프로젝트 변경 이력 테이블 표시
   - 날짜 기준 내림차순 정렬
   - 업무진행사항, 업무추가 일정계획, 애로사항 변경 내역 확인
   - 애로사항 개선 여부 표시

8. **페이지네이션**
   - 페이지 번호
   - 이전/다음 페이지 버튼
   - 페이지당 항목 수 선택기

---

## 4. 데이터 흐름

1. **초기 데이터 로드**
   - 서버 컴포넌트에서 Google Sheets API를 통해 `프로젝트` 시트 데이터 가져오기
   - `담당자` 시트 데이터 가져오기 (개발담당자 선택용)
   - `프로젝트 단계목록` 시트 데이터 가져오기 (개발업무단계 선택용)
   - 캐싱을 활용한 성능 최적화 (revalidate: 3600)

2. **프로젝트 목록 필터링 및 정렬**
   - 클라이언트 컴포넌트에서 사용자 필터 설정에 따라 데이터 필터링
   - 사용자 선택에 따라 결과 정렬 (클라이언트 측)
   - 페이지네이션 처리 (대용량 데이터 고려)

3. **프로젝트 등록 흐름**
   - [새 프로젝트 등록] 버튼 클릭 → 모달 표시
   - 폼 데이터 입력 및 유효성 검증
   - 저장 버튼 클릭 → Google Sheets API를 통해 `프로젝트` 시트에 데이터 추가
   - 성공/실패 피드백 제공
   - 성공 시 모달 닫기 및 프로젝트 목록 갱신 (revalidatePath/revalidateTag)

4. **프로젝트 수정 흐름**
   - 프로젝트 행 확장 → 수정 폼 표시
   - 필드 수정 및 유효성 검증
   - 저장 버튼 클릭 → Google Sheets API를 통해 `프로젝트` 시트의 해당 행 업데이트
   - 프로젝트 업무관련 필드("업무진행사항", "업무추가 일정계획", "애로사항")에 변경이 있는 경우:
     - 기존 데이터와 비교하여 변경 내역 확인
     - `프로젝트이력관리` 시트에 변경내역 기록 (날짜, ITEM, PART NO, 업무진행사항, 업무추가 일정계획, 애로사항, 애로사항 개선 여부)
   - 성공/실패 피드백 제공
   - 성공 시 행 축소 및 프로젝트 목록 갱신

5. **프로젝트 복제 흐름**
   - 복제 버튼 클릭 → 현재 프로젝트 데이터를 기반으로 새 프로젝트 생성
   - NO 필드는 자동 할당 (최대값 + 1)
   - 복제된 프로젝트를 `프로젝트` 시트에 새로운 행으로 추가
   - 성공/실패 피드백 제공
   - 성공 시 프로젝트 목록 갱신

6. **프로젝트 업무관리 흐름**
   - 업무관리 버튼 클릭 → 업무관리 페이지로 이동
   - 프로젝트 정보 로드 (ITEM, PART NO 등)
   - 현재 업무진행사항, 업무추가 일정계획, 애로사항 정보 로드
   - 수정 후 저장 버튼 클릭 → Google Sheets API를 통해 `프로젝트` 시트 업데이트
   - 변경 내역이 있는 경우 `프로젝트이력관리` 시트에 기록 (날짜, ITEM, PART NO, 업무진행사항, 업무추가 일정계획, 애로사항, 애로사항 개선 여부)
   - 이력 테이블 업데이트
   - 성공/실패 피드백 제공

---

## 5. 기술 스택 및 컴포넌트

### 프론트엔드 기술
- Next.js 서버 컴포넌트 / 클라이언트 컴포넌트 분리
- Shadcn UI 컴포넌트 라이브러리
  - `Dialog` 컴포넌트: 프로젝트 등록/수정 모달
  - `Form` 컴포넌트: 입력 폼 및 유효성 검증
  - `DataTable` 컴포넌트: 프로젝트 목록 표시
  - `DatePicker` 컴포넌트: 날짜 및 기간 선택
  - `MultiSelect` 컴포넌트: 개발담당자 다중 선택
  - `Button`, `Card` 등 기본 UI 컴포넌트
  - `Collapsible` 컴포넌트: 행 확장/축소 기능
- React Hook Form: 폼 상태 관리 및 유효성 검증
- Zod: 스키마 기반 폼 유효성 검증
- TanStack Table: 고급 테이블 기능 (정렬, 필터링, 페이지네이션)

### 백엔드 기술
- Next.js 서버 액션을 활용한 데이터 조회 및 저장
- Google Sheets API를 통한 데이터 접근
- 캐싱 전략을 활용한 성능 최적화
- 데이터 변경 감지 로직 구현 (이력 관리)

---

## 6. 구현 계획

### 1단계: 프로젝트 목록 화면 구현
- 프로젝트 관리 페이지 라우트 (/project) 설정
- Google Sheets API에서 프로젝트 데이터 가져오기
- 기본 테이블 레이아웃 구현
- 정렬 및 페이지네이션 구현
- 행 확장/축소 기능 구현

### 2단계: 프로젝트 등록 기능 구현
- 프로젝트 등록 폼 컴포넌트 구현
- 담당자 다중 선택 기능 구현
- 유효성 검증 로직 구현
- Google Sheets API 연동하여 데이터 저장 기능 구현
- 성공/실패 피드백 UI 구현

### 3단계: 프로젝트 필터링 및 수정 기능
- 다양한 필드 기반 필터링 컴포넌트 구현
- 프로젝트 수정 기능 구현
- 프로젝트 복제 기능 구현
- 변경사항 추적 및 저장 기능
- 프로젝트 이력 관리 기능 구현

### 4단계: 프로젝트 업무관리 기능
- 업무관리 페이지 구현
- 업무진행사항, 업무추가 일정계획, 애로사항 관리
- 이력 테이블 구현
- 변경 내역 감지 및 기록 로직 구현

### 5단계: UI/UX 개선 및 성능 최적화
- 사용자 피드백 메시지 개선
- 뷰 전환 기능 (테이블/카드) 구현
- 데이터 내보내기 기능 구현
- 캐싱 전략 최적화
- 대용량 데이터 처리 최적화

---

## 7. API 설계

### 프로젝트 데이터 조회 서버 액션

```typescript
// app/actions/project.ts
"use server";

import { unstable_cache as cache } from "next/cache";

export interface Project {
  id: string;
  no: number;
  status: 'progress' | 'hold' | 'completed'; // 진행여부 (진행, 보유, 완료)
  customer: string;
  item: string;
  partNo?: string;
  managers?: string[];
  developmentStages?: string[]; // 다중 선택 가능한 개발업무단계
  schedule?: { start?: string; end?: string } | null;
  progress?: string; // 업무진행사항
  additionalPlan?: string; // 업무추가 일정계획
  issues?: string; // 애로사항
  issueResolved?: boolean; // 애로사항 개선 여부
  sellingPrice?: number; // 판매가
  materialCost?: number; // 재료비
  materialCostRatio?: number; // 재료비율
  createdAt: string;
  updatedAt: string;
}

export interface ProjectHistory {
  id: string;
  date: string;
  item: string;
  partNo: string;
  progress?: string; // 업무진행사항
  additionalPlan?: string; // 업무추가 일정계획
  issues?: string; // 애로사항
  issueResolved?: boolean; // 애로사항 개선 여부
}

export interface ProjectListParams {
  filters?: {
    item?: string[];
    customer?: string[];
    managers?: string[];
    stage?: string[];
    currentStage?: string[];
  };
  sortBy?: string;
  sortDirection?: 'asc' | 'desc';
  page?: number;
  pageSize?: number;
}

export const getProjects = cache(
  async (params?: ProjectListParams) => {
    // Google Sheets API를 통한 프로젝트 데이터 조회
    // 필터링, 정렬, 페이지네이션 처리
    // ...
    return {
      data: [], // 프로젝트 데이터 배열
      total: 0, // 전체 결과 수
      page: 1,  // 현재 페이지
      pageSize: 20, // 페이지당 항목 수
    };
  },
  ["projects"],
  {
    revalidate: 3600,
    tags: ["projects"],
  }
);

export const getProject = cache(
  async (id: string) => {
    // 특정 ID의 프로젝트 상세 정보 조회
    // ...
    return null as Project | null;
  },
  ["project-detail"],
  {
    revalidate: 3600,
    tags: ["projects"],
  }
);

export const createProject = async (data: Omit<Project, 'id' | 'no' | 'createdAt' | 'updatedAt'>) => {
  try {
    // ... existing code ...
    
    // Google Sheets API로 데이터 전송
    const values = [
      [
        newNo.toString(), // NO (자동생성)
        statusMap[data.status] || '진행', // 진행여부
        data.customer,    // 고객사
        data.item,        // ITEM
        data.partNo || '', // PART NO
        managersStr,      // 개발담당
        stagesStr,        // 개발업무단계
        data.schedule ? `${data.schedule.start || ''} ~ ${data.schedule.end || ''}` : '', // 대일정
        '',               // 현재단계 (빈값으로 초기화)
        data.progress || '', // 업무진행사항
        data.additionalPlan || '', // 업무추가 일정계획
        data.issues || '', // 애로사항
        data.issueResolved ? 'O' : '', // 애로사항 개선 여부
        data.sellingPrice || '', // 판매가
        data.materialCost || '', // 재료비
        data.materialCostRatio || '', // 재료비율
        new Date().toISOString(), // createdAt
        new Date().toISOString(), // updatedAt
      ]
    ];
    
    // ... existing code ...
  }
};

export const updateProject = async (id: string, data: Partial<Project>, previousData?: Project) => {
  try {
    // 기존 프로젝트 업데이트 로직
    
    // 업무관련 필드 변경 감지
    if (previousData && 
       (data.progress !== previousData.progress || 
        data.additionalPlan !== previousData.additionalPlan || 
        data.issues !== previousData.issues ||
        data.issueResolved !== previousData.issueResolved)) {
      
      // 프로젝트이력관리 시트에 변경내역 추가
      await addProjectHistory({
        date: new Date().toISOString().split('T')[0], // YYYY-MM-DD 형식
        item: data.item || previousData.item,
        partNo: data.partNo || previousData.partNo || '',
        progress: data.progress,
        additionalPlan: data.additionalPlan,
        issues: data.issues,
        issueResolved: data.issueResolved
      });
    }
    
    return { success: true };
  } catch (error) {
    console.error('프로젝트 업데이트 오류:', error);
    return { success: false, error: error.message };
  }
};

export const getProjectHistory = cache(
  async (itemId: string, partNo?: string) => {
    // 프로젝트이력관리 시트에서 특정 ITEM+PART NO의 이력 조회
    // 날짜 기준 내림차순 정렬
    
    return [] as ProjectHistory[];
  },
  ["project-history"],
  {
    revalidate: 3600,
    tags: ["project-history"],
  }
);

export const addProjectHistory = async (data: Omit<ProjectHistory, 'id'>) => {
  try {
    // 프로젝트이력관리 시트에 새로운 이력 추가
    const values = [
      [
        data.date,
        data.item,
        data.partNo,
        data.progress || '',
        data.additionalPlan || '',
        data.issues || '',
        data.issueResolved ? 'O' : ''
      ]
    ];
    
    // Google Sheets API 호출
    
    return { success: true };
  } catch (error) {
    console.error('프로젝트 이력 추가 오류:', error);
    return { success: false, error: error.message };
  }
};
```

---

## 8. UI 컴포넌트 세부 설계

### ProjectFilterForm 컴포넌트
```typescript
interface ProjectFilterFormProps {
  defaultValues?: ProjectListParams['filters'];
  onSubmit: (filters: ProjectListParams['filters']) => void;
}

function ProjectFilterForm({ defaultValues, onSubmit }: ProjectFilterFormProps) {
  // React Hook Form을 사용한 필터 상태 관리
  // 여러 필터 필드 (ITEM, 고객사, 담당자, 업무단계, 현재단계 등) 구현
  // 필터 적용/초기화 기능
}
```

### ProjectList 컴포넌트
```typescript
interface ProjectListProps {
  projects: Project[];
  onDuplicate: (project: Project) => void;
  onNavigateToWorkManagement: (project: Project) => void;
}

function ProjectList({ projects, onDuplicate, onNavigateToWorkManagement }: ProjectListProps) {
  // ... existing code ...
  
  // 업무관리 페이지로 이동 핸들러
  const handleWorkManagementClick = (project: Project) => {
    onNavigateToWorkManagement(project);
  };
  
  // ... existing code ...
}
```

### ProjectWorkManagementForm 컴포넌트
```typescript
interface ProjectWorkManagementFormProps {
  project: Project;
  history: ProjectHistory[];
  onSubmit: (values: Partial<Project>) => Promise<void>;
  onCancel: () => void;
}

function ProjectWorkManagementForm({ project, history, onSubmit, onCancel }: ProjectWorkManagementFormProps) {
  // React Hook Form + Zod를 사용한 폼 상태 관리 및 유효성 검증
  
  // 기존 프로젝트 데이터를 기본값으로 사용
  const defaultValues = {
    progress: project.progress || '',
    additionalPlan: project.additionalPlan || '',
    issues: project.issues || '',
    issueResolved: project.issueResolved || false
  };
  
  // 업무진행사항, 업무추가 일정계획, 애로사항 입력 필드 구현
  // 애로사항 개선 여부 체크박스
  // 폼 제출 핸들링
  // 이력 테이블 렌더링
}
```

### ProjectHistoryTable 컴포넌트
```typescript
interface ProjectHistoryTableProps {
  history: ProjectHistory[];
}

function ProjectHistoryTable({ history }: ProjectHistoryTableProps) {
  // 프로젝트 이력 테이블 구현
  // 날짜별 내림차순 정렬
  // 업무진행사항, 업무추가 일정계획, 애로사항, 애로사항 개선 여부 표시
  // 테이블 형태로 표시
}
```

---

## 3. 시트 구조 및 연동 (Docs & Sheets)

### 📊 Google Spreadsheet 이름: **`프로젝트관리`**

#### 📄 업무일지 시트 (`업무일지`)
| 날짜 | ITEM | PART NO | 단계 | 담당자 | 계획 | 실적 | 비고 |
|------|------|----------|------|--------|------|------|------|

#### 📄 프로젝트 시트 (`프로젝트`)
| NO | 단계 | 고객사 | 개발담당 | ITEM | PART NO | 개발업무단계 | 대일정 | 현재단계 | 업무진행사항 | 애로사항 | 비고 | 업무추가 일정계획 | 판매가 | 재료비 | 재료비율 |
|----|------|--------|-----------|------|----------|----------------|--------|-----------|----------------|----------|------|--------------------|--------|--------|----------|

#### 📄 담당자 시트 (`담당자`)
| 직급 | 이름 |
|------|------|

#### 📄 프로젝트이력관리 시트 (`프로젝트이력관리`)
| 날짜 | ITEM | PART NO | 업무진행사항 | 업무추가 일정계획 | 애로사항 | 애로사항 개선 여부 |
|------|------|----------|--------------|------------------|----------|-------------------|

===========
난이도: 중간  
학습 키워드: Next.js, Google Sheets API, 서버 컴포넌트, 클라이언트 컴포넌트, React Hook Form, Zod, 데이터 캐싱, 가상화, 상태 관리
===========
           
